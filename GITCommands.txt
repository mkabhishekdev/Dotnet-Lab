[WHEN GENERATING A PDF USING CHATGPT, FIRST GO THROUGH THE NOTES AND ADD SOME MORE DETAILS REFERRING
                  CHATGPT LIKE HOW TO REMEMBER SOME OF THE CONCEPTS WHEN EXPLAINED IN SIMPLER OR VISUAL WAY,
                  ALSO GO THROUGH YOUR NOTES BEFORE GENERATING PDF TO PRINT - USE SOME CHATGPT THINGS LIKE
                  ADDING SOME MORE INFORMATION FOR YOU TO REMEMBER THE CONCEPTS ALWAYS. LIKE REVISIT A 
                  SECTION BELOW - ASK IT TO GENERATE SOMETHING VISUALLY TO UNDERSTAND WELL OR TELL THE 
                  CONCEPT IN VERY SIMPLE WORDS]

USE ONLY GITHUB CLI(Command line interface)
---------------------------------------------

1. 
   - When cloning a repo using terminal: you should first type "git clone" and than paste the repo

   - How to create branch from GitCLI?
       - make sure you are in the main parent branch & pull the latest
             git checkout -b [branch-name] main
             git push -u origin [branch-name]
             gh repo view --web //command to open the web-view

    - To check branch: git branch
      To check status: git status

*******************************

2. To push changes from your local branch
       git add .
       git commit -m "enter the message"
       git push --set-upstream origin <branch-name>
       * check now in github it will ask to create PR
       Another option:
       In command line you can enter the below -
           gh pr create --base main --head <branch-name> --title "<your message>" --body "<your message>"
       After the above line: GitHub will give you a link to view the PR.

******************************

3. If you updated your 'local branch' but not sure if you got latest from main branch
       - Ensure your local branch is first committed
             git add .
             git commit -m "message"
       - git checkout main
         git pull origin main
         git merge <your local branch>
         git push origin main 

********************************

4. Difference between git merge and git rebase?

🔹 git merge

What it does: Takes another branch (say main) and merges it into your branch (feat/data-structures).

It creates a new commit called a "merge commit".

Keeps the full history (with all branches).

Example:
git checkout feat/data-structures
git merge main


Result in history:

A---B---C (main)
         \
          D---E---M (feat/data-structures)


M is the merge commit.

Easy, safe, preserves context.

But history looks more “branchy” and can get messy in big teams.


🔹 git rebase

What it does: Takes your commits and “replays” them on top of another branch (say main).

Rewrites commit history to make it look like you built your feature after the latest main.

Example:
git checkout feat/data-structures
git rebase main


Result in history:

A---B---C (main)
             \
              D'---E' (feat/data-structures rebased)


No merge commit.

Cleaner, linear history.

But: rewrites commits (D → D’, E → E’), which can cause issues if others already pulled your branch.

✅ When to Use What

Merge:

Collaborative work
Safe, no commit rewriting
Good for resolving conflicts together

Rebase:

Personal branches not yet shared with team
You want a clean, linear history
Often used before merging a PR (git rebase main then squash commits)

*****************************

5. MERGE Conflict: If you don’t need your local changes at all

# Step 1: Discard local changes completely
git reset --hard

# Step 2: Clean up build-generated files
git clean -fd

# Step 3: Get the latest version from main
git fetch origin
git checkout main
git pull origin main

********************************

6. To Switch to Another Branch ignoring changes in the present branch which you don't want

   git restore .
   git checkout main

********************************


             
